# Lesson Levels

Offline lesson content is grouped by level:

- `level1/` keeps the original Learn Portuguese lesson set.
- `level2/` contains the Learn Portuguese 2 lesson set.

Each level currently uses ten lesson files named `lesson01.json` through `lesson10.json`.
